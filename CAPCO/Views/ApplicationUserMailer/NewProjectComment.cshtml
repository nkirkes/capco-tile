@{
    Layout = "~/Views/ApplicationUserMailer/_Layout.cshtml";
    CAPCO.Infrastructure.Domain.ProjectComment comment = ViewBag.Comment;
}

<h2>@ViewBag.Title</h2>

<p>
    Hello! @comment.CreatedBy.FullName has commented on the @comment.Project.ProjectName project. The comment details are below:</p>
<p>
    <pre>
        @comment.Text
    </pre><br />
    <em>on @comment.CreatedOn.ToShortDateString()</em>
</p>
    
<p>To view the comment and the associated project, follow the link below: </p>

<a href="@Url.HttpRouteUrl("/projects/", null)@comment.ProjectId">@Url.HttpRouteUrl("/projects/", null)@comment.ProjectId</a>

<p>
    If the link doesn't work, please try entering the url into your web browser manually
    using copy/paste or by typing it in.</p>

<p>
    Thanks!</p>

~ The CAPCO Team
