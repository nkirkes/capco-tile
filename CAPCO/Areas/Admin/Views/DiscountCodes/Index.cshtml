@model IEnumerable<CAPCO.Infrastructure.Domain.DiscountCode>

@{
    ViewBag.Title = "Manage Discount Codes";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("New Discount Code", "New")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Name
        </th>
        <th>
            Code
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id })
        </td>
        <td>
			@item.Name
        </td>
        <td>
			@item.Code
        </td>
    </tr>
}

</table>


