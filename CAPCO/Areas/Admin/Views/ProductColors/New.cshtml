@model CAPCO.Infrastructure.Domain.ProductColor

@{
    ViewBag.Title = "New Product Color";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("create", "productcolors", FormMethod.Post)) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>ProductColor</legend>

		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


