@model CAPCO.Infrastructure.Domain.ProductSeries

@{
    ViewBag.Title = "Create Product Series";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("create", "productseries", FormMethod.Post)) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Product Series</legend>

		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


