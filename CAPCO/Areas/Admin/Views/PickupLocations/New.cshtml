@model CAPCO.Infrastructure.Domain.PickupLocation

@{
    ViewBag.Title = "New Pickup Location";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("create", "pickuplocations", FormMethod.Post)) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Pickup Location</legend>

		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


