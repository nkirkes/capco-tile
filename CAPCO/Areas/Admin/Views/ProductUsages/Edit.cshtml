@model CAPCO.Infrastructure.Domain.ProductUsage

@{
    ViewBag.Title = "Edit Product Usage";
}

<h2>Edit</h2>

@using (Html.BeginForm("update", "productusages", new { id = Model.Id }, FormMethod.Post, null)) {
    @Html.ValidationSummary(true)
	@Html.AntiForgeryToken()
    @Html.HttpMethodOverride(HttpVerbs.Put)
    <fieldset>
        <legend>Product Usage</legend>

		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index") | @Html.ActionLink("Delete This Product Usage", "delete", "productusages", new { area = "admin", id = Model.Id }, new { onclick = "javascript: return confirm(\"Are you sure? This cannot be undone.\");" }) 
</div>
