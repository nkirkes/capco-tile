@model CAPCO.Infrastructure.Domain.ProductCategory

@{
    ViewBag.Title = "Edit Product Category";
}

<h2>Edit</h2>

@using (Html.BeginForm("update", "productcategories", new { id = Model.Id, area = "admin" }, FormMethod.Post, null))
{
    @Html.ValidationSummary(true)
	@Html.AntiForgeryToken()
    @Html.HttpMethodOverride(HttpVerbs.Put)
    <fieldset>
        <legend>Product Category</legend>

		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index") | @Html.ActionLink("Delete This Product Category", "delete", "productcategories", new { area = "admin", id = Model.Id }, new { onclick = "javascript: return confirm(\"Are you sure? This cannot be undone.\");" }) 
</div>
